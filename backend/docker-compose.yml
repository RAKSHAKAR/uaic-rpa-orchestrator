services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: uaic_backend_standalone
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped